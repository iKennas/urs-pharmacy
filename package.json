{
  "name": "urs-pharmacy",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "npm --prefix backend ci && npm --prefix backend exec prisma generate && npm --prefix backend run build",
    "start": "node backend/dist/src/main.js"
  }
}
