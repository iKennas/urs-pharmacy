# Set automatically by Railway when a Postgres plugin is attached:
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/railway

# Generate with: openssl rand -hex 32
JWT_SECRET=
JWT_EXPIRES_IN=8h

# Railway injects PORT; leave unset locally
# PORT=3001
